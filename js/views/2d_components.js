function CPolygon(vertex_list, line_style, fill_style)
{
    this.vertex_list = vertex_list;
    this.line_style = line_style;
    this.fill_style = fill_style;
}

function CText(text, center, font, font_style)
{
    this.text = text;
    this.center = center;
    this.font = font;
    this.font_style = font_style;
}